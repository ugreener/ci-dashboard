apiVersion: v1
kind: Service
metadata:
  name: ci-dashboard
  labels:
    app: ci-dashboard
    paas.redhat.com/appcode: ISVD-001
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: ci-dashboard
  type: ClusterIP
